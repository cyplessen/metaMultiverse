% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_plot_input.R
\name{resolve_plot_input}
\alias{resolve_plot_input}
\title{Resolve the results table and factor columns for a plot (internal helper)}
\usage{
resolve_plot_input(
  x,
  factors = NULL,
  required = c("b", "ci.lb", "ci.ub", "pval", "k"),
  use_default = TRUE,
  require_factors = TRUE
)
}
\arguments{
\item{x}{A \code{multiverse_result}, a \code{pre_post_multiverse} or a data
frame of results.}

\item{factors}{\code{NULL} or a character vector of factor column names,
optionally named with display labels.}

\item{required}{Character vector of result columns that must be present.}

\item{use_default}{Logical. When \code{factors} is \code{NULL}, use the
input's own factor columns (\code{TRUE}) or no factors at all
(\code{FALSE}).}

\item{require_factors}{Logical. Error when no factor column can be found.}
}
\value{
A list with \code{data} (the results data frame, factor columns
  coerced to character), \code{factors} (character vector of factor column
  names), \code{labels} (named list of display labels, one per factor) and
  \code{factor_groups} (custom group definitions, or \code{NULL}).
}
\description{
Resolve the results table and factor columns for a plot (internal helper)
}
\keyword{internal}
