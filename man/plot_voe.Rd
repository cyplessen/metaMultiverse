% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_VoE.R
\name{plot_voe}
\alias{plot_voe}
\title{Vibration of Effects Plot}
\usage{
plot_voe(
  x,
  x_var = "b",
  y_var = "pval",
  colorblind_friendly = TRUE,
  cutoff = 5,
  x_breaks = NULL,
  y_breaks = c(1e-11, 1e-10, 1e-09, 1e-08, 1e-07, 1e-06, 1e-05, 1e-04, 0.001, 0.01, 0.05,
    0.1, 0.5, 1),
  x_limits = NULL,
  y_limits = c(1e-11, 1),
  vertical_lines = c(0.1, 0.9),
  hline_value = 0.05,
  title_template = "{k} meta-analyses with at least {cutoff} studies",
  interactive = TRUE
)
}
\arguments{
\item{x}{A multiverse_result object from \code{\link{run_multiverse_analysis}}}

\item{x_var}{Column name for x-axis values. Default: "b" (effect size)}

\item{y_var}{Column name for y-axis values. Default: "pval" (p-value)}

\item{colorblind_friendly}{Logical. Use colorblind-friendly palette. Default: TRUE}

\item{cutoff}{Minimum number of studies required for inclusion. Default: 5}

\item{x_breaks}{Numeric vector of x-axis break points. Default: NULL (auto-generated)}

\item{y_breaks}{Numeric vector of y-axis break points for log scale.
Default: c(1e-11, 1e-10, 1e-9, 1e-8, 1e-7, 1e-6, 1e-5, 1e-4, 0.001, 0.01, 0.05, 0.1, 0.5, 1)}

\item{x_limits}{Numeric vector of length 2 for x-axis limits. Default: NULL (auto-calculated)}

\item{y_limits}{Numeric vector of length 2 for y-axis limits. Default: c(1e-11, 1)}

\item{vertical_lines}{Numeric vector of quantiles for vertical reference lines. Default: c(0.1, 0.9)}

\item{hline_value}{Y-value for horizontal reference line. Default: 0.05}

\item{title_template}{Character string with glue syntax for plot title.
Available variables: k (number of analyses), cutoff.
Default: "{k} meta-analyses with at least {cutoff} studies"}

\item{interactive}{Logical. Return interactive plotly (TRUE) or static ggplot2 (FALSE). Default: TRUE}
}
\value{
A plotly object if interactive = TRUE, otherwise a ggplot2 object
}
\description{
Visualizes the distribution of effect sizes and p-values across multiverse specifications.
Points are colored by density to highlight concentration areas. Automatically handles
multiple multiverses when non-equivalent factors are present.
}
\details{
The plot displays:
\itemize{
  \item Points colored by density (high-density areas highlighted)
  \item Log-scaled y-axis for p-values
  \item Vertical lines at specified quantiles of the effect size distribution
  \item Horizontal line at significance threshold (default: p = 0.05)
  \item Automatic faceting when multiple multiverses exist (from N-type factors)
}
}
\examples{
\dontrun{
# Basic usage
multiverse \%>\% plot_voe()

# Customize significance threshold and quantiles
multiverse \%>\%
  plot_voe(hline_value = 0.01,
          vertical_lines = c(0.25, 0.75))

# Static plot with custom limits
multiverse \%>\%
  plot_voe(interactive = FALSE,
          x_limits = c(-0.5, 1.5),
          cutoff = 10)
}

}
\seealso{
\code{\link{plot_spec_curve}} for specification curve visualization
\code{\link{run_multiverse_analysis}} for generating input data
}
